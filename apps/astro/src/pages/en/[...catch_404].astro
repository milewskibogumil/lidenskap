---
import NotFoundPage, { fetchData } from '@templates/404.astro'
import { LANGUAGES } from '@utils/languages'

const lang = 'en'
const data = await fetchData(lang)
if (!LANGUAGES.includes(lang) || !data) return Astro.rewrite('/404')
---

<NotFoundPage {...data} />
