/**
 * Global declaration of the domain for the application.
 * This constant is used for constructing full URLs and determining external links.
 * @constant
 * @type {string}
 */
export const DOMAIN: string = "https://lidenskap.pl";

/**
 * Sanity project ID used for connecting to the Sanity.io service.
 * This ID is used across configuration files to maintain consistency.
 * @constant
 * @type {string}
 */
export const PROJECT_ID: string = "0qbklpoh";

/**
 * Sanity dataset name that specifies which dataset to use.
 * This value is used across configuration files to maintain consistency.
 * @constant
 * @type {string}
 */
export const DATASET: string = "production";

